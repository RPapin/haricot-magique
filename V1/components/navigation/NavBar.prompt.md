En-tête du site: signature à gauche, liens au centre-droit, un seul bouton.

```jsx
<NavBar links={[{id:'ferme',label:'La ferme'},{id:'legumes',label:'Les légumes'}]} active="ferme" onNavigate={setVue} />
```
