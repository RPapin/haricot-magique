Bouton icône seule, toujours circulaire, toujours avec `label`.

```jsx
<IconButton icon="basket" label="Panier" tone="soft" />
```

Tons: `neutral` (filet), `solid` (basilic plein), `soft` (crème). 32/42/52 px.
